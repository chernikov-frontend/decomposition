/**
 * Search – компонент строки поиска.
 */

const Search = () => {
    return (
        <div>
        <input type="text" placeholder="Введите запрос..." />
        <button>Найти</button>
        </div>
    );
};

export default Search;
